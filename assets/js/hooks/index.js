/**
 * Hooks index - Aggregates all Phoenix LiveView hooks
 */
import Clipboard from "./clipboard"
import { MentionInput, LocalTime, ScrollToBottom } from "./ask_anything"

const Hooks = {
    Clipboard,
    MentionInput,
    LocalTime,
    ScrollToBottom
}

export default Hooks
